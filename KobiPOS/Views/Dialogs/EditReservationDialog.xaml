<Window x:Class="KobiPOS.Views.Dialogs.EditReservationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rezervasyon Düzenle"
        Width="500"
        Height="750"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#ECEFF1">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- BAŞLIK -->
        <TextBlock Grid.Row="0"
                   Text="✏️ REZERVASYON DÜZENLE"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="#1976D2"
                   Margin="0,0,0,20"
                   HorizontalAlignment="Center"/>
        
        <!-- FORM -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- MÜŞTERİ ADI -->
                <TextBlock Text="Müşteri Adı *" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="CustomerNameTextBox"
                         Text="{Binding CustomerName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="14"
                         Padding="10"
                         Margin="0,0,0,15"
                         BorderThickness="1"
                         BorderBrush="#90CAF9"/>
                
                <!-- TELEFON -->
                <TextBlock Text="Telefon *" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="CustomerPhoneTextBox"
                         Text="{Binding CustomerPhone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="14"
                         Padding="10"
                         Margin="0,0,0,15"
                         BorderThickness="1"
                         BorderBrush="#90CAF9"/>
                
                <!-- E-POSTA -->
                <TextBlock Text="E-posta (Opsiyonel)" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="CustomerEmailTextBox"
                         Text="{Binding CustomerEmail, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="14"
                         Padding="10"
                         Margin="0,0,0,15"
                         BorderThickness="1"
                         BorderBrush="#90CAF9"/>
                
                <!-- KİŞİ SAYISI -->
                <TextBlock Text="Kişi Sayısı *" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ComboBox x:Name="GuestCountComboBox"
                          FontSize="14"
                          Padding="10"
                          Margin="0,0,0,15">
                    <ComboBoxItem Content="1"/>
                    <ComboBoxItem Content="2"/>
                    <ComboBoxItem Content="3"/>
                    <ComboBoxItem Content="4"/>
                    <ComboBoxItem Content="5"/>
                    <ComboBoxItem Content="6"/>
                    <ComboBoxItem Content="7"/>
                    <ComboBoxItem Content="8"/>
                    <ComboBoxItem Content="9"/>
                    <ComboBoxItem Content="10"/>
                    <ComboBoxItem Content="12"/>
                </ComboBox>
                
                <!-- TARİH -->
                <TextBlock Text="Rezervasyon Tarihi *" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <DatePicker x:Name="ReservationDatePicker"
                            SelectedDate="{Binding ReservationDate, Mode=TwoWay}"
                            FontSize="14"
                            Padding="10"
                            Margin="0,0,0,15"/>
                
                <!-- SAAT -->
                <TextBlock Text="Rezervasyon Saati *" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ComboBox x:Name="ReservationTimeComboBox"
                          FontSize="14"
                          Padding="10"
                          Margin="0,0,0,15">
                    <ComboBoxItem Content="09:00" Tag="09:00:00"/>
                    <ComboBoxItem Content="10:00" Tag="10:00:00"/>
                    <ComboBoxItem Content="11:00" Tag="11:00:00"/>
                    <ComboBoxItem Content="12:00" Tag="12:00:00"/>
                    <ComboBoxItem Content="13:00" Tag="13:00:00"/>
                    <ComboBoxItem Content="14:00" Tag="14:00:00"/>
                    <ComboBoxItem Content="15:00" Tag="15:00:00"/>
                    <ComboBoxItem Content="16:00" Tag="16:00:00"/>
                    <ComboBoxItem Content="17:00" Tag="17:00:00"/>
                    <ComboBoxItem Content="18:00" Tag="18:00:00"/>
                    <ComboBoxItem Content="19:00" Tag="19:00:00"/>
                    <ComboBoxItem Content="20:00" Tag="20:00:00"/>
                    <ComboBoxItem Content="21:00" Tag="21:00:00"/>
                    <ComboBoxItem Content="22:00" Tag="22:00:00"/>
                    <ComboBoxItem Content="23:00" Tag="23:00:00"/>
                </ComboBox>
                
                <!-- MASA SEÇİMİ -->
                <TextBlock Text="Masa Seçimi *" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ComboBox x:Name="TableComboBox"
                          ItemsSource="{Binding AvailableTables}"
                          SelectedValue="{Binding TableID, Mode=TwoWay}"
                          DisplayMemberPath="DisplayName"
                          SelectedValuePath="ID"
                          FontSize="14"
                          Padding="10"
                          Margin="0,0,0,15"/>
                
                <!-- DURUM -->
                <TextBlock Text="Durum *" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ComboBox x:Name="StatusComboBox"
                          FontSize="14"
                          Padding="10"
                          Margin="0,0,0,15">
                    <ComboBoxItem Content="⏳ Bekliyor" Tag="Pending"/>
                    <ComboBoxItem Content="✅ Onaylandı" Tag="Confirmed"/>
                    <ComboBoxItem Content="✔️ Tamamlandı" Tag="Completed"/>
                    <ComboBoxItem Content="❌ İptal Edildi" Tag="Cancelled"/>
                    <ComboBoxItem Content="🚫 Gelmedi" Tag="NoShow"/>
                </ComboBox>
                
                <!-- NOTLAR -->
                <TextBlock Text="Notlar (Opsiyonel)" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="NotesTextBox"
                         Text="{Binding Notes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="14"
                         Padding="10"
                         Height="80"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Margin="0,0,0,15"
                         BorderThickness="1"
                         BorderBrush="#90CAF9"/>
            </StackPanel>
        </ScrollViewer>
        
        <!-- BUTONLAR -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="💾 Güncelle"
                    Click="SaveButton_Click"
                    Background="#4CAF50"
                    Foreground="White"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Padding="30,12"
                    Margin="0,0,10,0"
                    BorderThickness="0"
                    Cursor="Hand"/>
            
            <Button Content="❌ İptal"
                    Click="CancelButton_Click"
                    Background="#F44336"
                    Foreground="White"
                    FontSize="16"
                    Padding="30,12"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
