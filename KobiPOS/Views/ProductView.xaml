<UserControl x:Class="KobiPOS.Views.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" 
                Padding="20" Margin="0,0,0,10">
            <TextBlock Text="Ürün Yönetimi" 
                       FontSize="24" 
                       FontWeight="Bold"/>
        </Border>

        <!-- Categories -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" 
                Padding="15" Margin="0,0,0,10">
            <ItemsControl ItemsSource="{Binding Categories}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding CategoryName}"
                                Style="{StaticResource PrimaryButton}"
                                Margin="5,0"
                                Padding="20,10"
                                Command="{Binding DataContext.RefreshCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <!-- Products List -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" Padding="10">
            <DataGrid ItemsSource="{Binding Products}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      CanUserResizeRows="False"
                      AlternatingRowBackground="#F9F9F9">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ürün Adı" 
                                        Binding="{Binding ProductName}" 
                                        Width="*"/>
                    <DataGridTextColumn Header="Fiyat" 
                                        Binding="{Binding Price, StringFormat='{}{0:N2} ₺'}" 
                                        Width="150"/>
                    <DataGridTextColumn Header="Stok" 
                                        Binding="{Binding StockQuantity}" 
                                        Width="100"/>
                    <DataGridTextColumn Header="Barkod" 
                                        Binding="{Binding Barcode}" 
                                        Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
