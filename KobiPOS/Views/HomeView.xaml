<UserControl x:Class="KobiPOS.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{StaticResource BackgroundBrush}">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="KobiPOS" 
                       FontSize="48" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Foreground="{StaticResource PrimaryBrush}"/>
            
            <TextBlock Text="Cafe &amp; Restoran Yönetim Sistemi" 
                       FontSize="20"
                       HorizontalAlignment="Center"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,10,0,40"/>

            <Border Background="{StaticResource SurfaceBrush}" 
                    Padding="40,30"
                    CornerRadius="8"
                    MaxWidth="600">
                <StackPanel>
                    <TextBlock Text="Hoş Geldiniz!" 
                               FontSize="24" 
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,20"/>
                    
                    <TextBlock TextWrapping="Wrap" 
                               FontSize="14"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center">
                        Profesyonel cafe ve restoran yönetim sisteminiz hazır.
                        <LineBreak/>
                        Soldaki menüden istediğiniz bölüme geçebilirsiniz.
                    </TextBlock>

                    <Separator Margin="0,20"/>

                    <TextBlock Text="Hızlı Erişim" 
                               FontWeight="SemiBold"
                               Margin="0,10"/>
                    
                    <WrapPanel HorizontalAlignment="Center" Margin="0,10">
                        <Border Background="{StaticResource SuccessBrush}" 
                                Padding="20,15" 
                                Margin="5"
                                CornerRadius="4">
                            <TextBlock Text="🍽️ Masalar" 
                                       Foreground="White"
                                       FontWeight="SemiBold"/>
                        </Border>
                        
                        <Border Background="{StaticResource PrimaryBrush}" 
                                Padding="20,15" 
                                Margin="5"
                                CornerRadius="4">
                            <TextBlock Text="📋 Ürünler" 
                                       Foreground="White"
                                       FontWeight="SemiBold"/>
                        </Border>
                        
                        <Border Background="{StaticResource AccentBrush}" 
                                Padding="20,15" 
                                Margin="5"
                                CornerRadius="4">
                            <TextBlock Text="📊 Raporlar" 
                                       Foreground="White"
                                       FontWeight="SemiBold"/>
                        </Border>
                    </WrapPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
