<UserControl x:Class="KobiPOS.Views.ReservationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#ECEFF1">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- BAŞLIK -->
        <TextBlock Grid.Row="0" 
                   Text="📅 REZERVASYONLAR"
                   FontSize="28"
                   FontWeight="Bold"
                   Foreground="#1976D2"
                   Margin="0,0,0,20"/>
        
        <!-- ÜST BUTONLAR -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="➕ Yeni Rezervasyon"
                    Command="{Binding AddReservationCommand}"
                    Background="#4CAF50"
                    Foreground="White"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Padding="20,12"
                    Margin="0,0,10,0"
                    BorderThickness="0"
                    Cursor="Hand"/>
            
            <Button Content="🔄 Yenile"
                    Command="{Binding RefreshCommand}"
                    Background="#2196F3"
                    Foreground="White"
                    FontSize="16"
                    Padding="15,12"
                    Margin="0,0,10,0"
                    BorderThickness="0"
                    Cursor="Hand"/>
            
            <TextBlock Text="Tarih Filtresi:"
                       VerticalAlignment="Center"
                       FontSize="14"
                       Margin="20,0,10,0"/>
            
            <DatePicker SelectedDate="{Binding FilterDate, Mode=TwoWay}"
                        Width="150"
                        Margin="0,0,10,0"/>
        </StackPanel>
        
        <!-- REZERVASYON LİSTESİ -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Reservations}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F5F5F5"
                  RowHeight="50"
                  HeadersVisibility="Column">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50"/>
                
                <DataGridTextColumn Header="Müşteri Adı" Binding="{Binding CustomerName}" Width="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="14"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <DataGridTextColumn Header="Telefon" Binding="{Binding CustomerPhone}" Width="120"/>
                
                <DataGridTextColumn Header="Tarih" Binding="{Binding FormattedDate}" Width="100"/>
                
                <DataGridTextColumn Header="Saat" Binding="{Binding FormattedTime}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="16"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <DataGridTextColumn Header="Kişi Sayısı" Binding="{Binding GuestCount}" Width="80"/>
                
                <DataGridTextColumn Header="Masa" Binding="{Binding TableName}" Width="100"/>
                
                <DataGridTextColumn Header="Bölge" Binding="{Binding SectionName}" Width="100"/>
                
                <DataGridTemplateColumn Header="Durum" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding StatusColor}"
                                    CornerRadius="15"
                                    Padding="10,5"
                                    HorizontalAlignment="Center">
                                <TextBlock Text="{Binding StatusText}"
                                           Foreground="White"
                                           FontWeight="SemiBold"
                                           FontSize="12"
                                           HorizontalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                <DataGridTemplateColumn Header="İşlemler" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="✏️ Düzenle"
                                        Command="{Binding DataContext.EditReservationCommand, 
                                                RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Background="#2196F3"
                                        Foreground="White"
                                        Padding="8,5"
                                        Margin="0,0,5,0"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        FontSize="11"/>
                                
                                <Button Content="❌ İptal"
                                        Command="{Binding DataContext.CancelReservationCommand, 
                                                RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Background="#F44336"
                                        Foreground="White"
                                        Padding="8,5"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        FontSize="11"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
